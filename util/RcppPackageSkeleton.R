# This is all the code needed to add boilerplate files to your RStudio project (not created as a package!) To make it into a package.
# Note: This makes the package in a directory inside the RStudio project. You must then move the files up into the main directory of the project.
library(Rcpp)
setwd("/Users/stevenshuken/Documents/Research/P-value correction project/SIMPLYCORRECT Supplementary Code/permFDP/")
Rcpp.package.skeleton("RcppPackage")
